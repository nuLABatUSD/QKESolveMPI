#ifndef FINDISSUES_H
#define FINDISSUES_H

#include <iostream>
#include <fstream>
#include <string>
#include <regex>
#include <map>
#include <vector>
#include <algorithm>

#include <cmath>
#include <iomanip>
#include <sstream>
/*
int errorSum;
int errorIndex;

class issues{
    private:
        std::string indexValues [4];
    public:
        struct dsmRecord;

        void mostCommond(std::ofstream& out, const map<string, int>& counts, const string& label);
        void indexRanges(std::ofstream& out, const map<int, int>& indexCounts);
        void writeCommonDsmRanges(std::ofstream& out, map<int, int>& dsmRoundedCounts);
        void writeTopDsmValues(std::ofstream& out, vector<dsmRecord> records);
        int FindIssues();

};
*/

#endif